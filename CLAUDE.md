# CLAUDE.md — sluicery プロジェクトルール

本書は sluicery の開発における作業ルールである。リポジトリのルートに `CLAUDE.md` として配置し、**セッション開始時に必ず読むこと。**

## 0. 優先順位

指示が競合した場合、以下の順で優先する。

1. ユーザーからの直接の指示（当該セッション内）
2. 本書（CLAUDE.md）
3. `docs/要件定義.md`
4. `docs/基本設計.md`

要件定義に反する実装が必要になった場合、**勝手に進めず、必ず確認を取る。** 合意が取れたら要件定義側を先に更新してから実装する。

---

# 1. ドキュメント体系

| ファイル | 役割 | 性質 |
|---|---|---|
| `AISTATE.md` | **現在地の記録。** セッションを跨いだ引き継ぎ用 | 頻繁に更新。常に最新 |
| `docs/要件定義.md` | 何を作るか（WHAT） | 変更は慎重。合意を経てから |
| `docs/基本設計.md` | どう作るか（HOW）。構造・判断の記録 | 実装に合わせて随時 |
| `docs/変更履歴.md` | 変更の時系列記録（CHANGELOG） | 変更のたびに追記 |
| `README.md` | 利用者向け。導入と使い方 | 機能追加時 |
| `docs/footprint.md` | ホスト上に作られるものの一覧 | compose 変更時 |
| `docs/storage.md` | ストレージ方式の解説とトレードオフ | 該当機能の変更時 |
| `docs/legal.md` | 利用上の注意 | 初期作成後は基本固定 |

**`AISTATE.md` のみリポジトリルートに置く。** セッション開始時に真っ先に開くファイルであるため、階層に埋めない。

## 1.1 既存文書からの変更

- 初期に受領した `requirements.md` は `docs/要件定義.md` にリネームして使用する
- 要件定義書 §0 に記載のあった `docs/decisions.md` は作成しない。**設計判断は `docs/基本設計.md` の「設計判断の記録」セクションに集約する**

---

# 2. ドキュメントの更新ルール

## 2.1 更新トリガ

| 出来事 | 更新するファイル |
|---|---|
| 実装タスクを1つ完了した | `AISTATE.md`、`docs/変更履歴.md` |
| 新しいモジュール・テーブル・インターフェースを追加した | `docs/基本設計.md`、`AISTATE.md` |
| 要件定義に書かれていない設計判断をした | `docs/基本設計.md`（設計判断の記録）、`AISTATE.md` |
| 要件を変更・追加した（ユーザー合意済み） | `docs/要件定義.md`、`docs/変更履歴.md`、`AISTATE.md` |
| 未解決の問題・保留事項が発生した | `AISTATE.md` |
| 依存関係・環境構成を変更した | `docs/基本設計.md`、`README.md`、`AISTATE.md` |
| compose / Dockerfile を変更した | `docs/footprint.md`、`docs/変更履歴.md` |
| セッションを終える | `AISTATE.md`（必須） |

**「後でまとめて書く」を禁止する。** 作業の区切りごとに更新し、更新をコミットに含める。

## 2.2 変更履歴.md の書式

[Keep a Changelog](https://keepachangelog.com/) に準拠する。

```markdown
# 変更履歴

## [未リリース]

### 追加
- Storage アダプタの接続テスト機能（4段階判定）

### 変更
- Target の状態遷移に `blocked` を追加。容量不足時にリトライ回数を消費しないよう変更

### 修正
- relink 時に大文字小文字を区別していた問題を修正

### 削除
- （なし）

## [0.1.0] - 2026-08-15
...
```

- 日本語で記述する
- 「なぜ」が非自明な場合は1行添える
- コミットハッシュは書かない（git log で辿れるため）

## 2.3 基本設計.md の構成

以下の章立てを維持する。

```
1. 全体構成図（サービス、データフロー）
2. モジュール構成と責務
3. データモデル（実装済みスキーマ。要件定義の論理設計との差分を明記）
4. 主要なフローのシーケンス（同期、パイプライン、relink）
5. 拡張点の実装状況
6. 外部依存（yt-dlp / rclone / ffmpeg）との境界
7. 設計判断の記録
```

「7. 設計判断の記録」は以下の形式で追記する。

```markdown
### D-007: Staging の孤立ファイル判定に mtime を併用する

- 日付: 2026-08-20
- 背景: Task テーブルとの突合だけでは、DB 復元直後に正常なファイルを孤立と誤判定する
- 選択肢: (a) Task 突合のみ (b) mtime 併用 (c) sidecar ファイル
- 決定: (b)。24時間以内に更新されたファイルは孤立判定から除外する
- 影響: 要件定義 §6.3 の記述を補完するもので、矛盾はしない
```

---

# 3. AISTATE.md

## 3.1 目的

会話のコンテキスト長が限界に達して新しいセッションを開始した場合でも、**このファイルを読めば作業を再開できる**状態を保つ。

想定する読み手は「このプロジェクトを初めて見る自分」である。要件定義や基本設計を全部読み返さなくても、現在地・次の一手・注意点が分かるように書く。

## 3.2 運用ルール

- **セッション終了時は必ず更新する**（最重要）
- タスク完了ごとに更新する
- **常に全文を書き換える。** 追記して肥大化させない
- **300行を超えないよう剪定する。** 詳細が必要になったら他ドキュメントへ移し、AISTATE からは参照だけ残す
- 完了した作業の詳細は残さない。「何が終わったか」の一行だけ残し、詳細は `docs/変更履歴.md` に移す
- 憶測を書かない。確認していないことは「未確認」と明記する

## 3.3 テンプレート

以下の構成を維持すること。初回作成時にこのまま使う。

```markdown
# AISTATE

> このファイルはセッション間の引き継ぎ用です。
> セッション開始時に最初に読み、セッション終了時に必ず更新してください。

最終更新: YYYY-MM-DD HH:MM
対応コミット: <短縮ハッシュ> <コミットメッセージ1行目>

## プロジェクト概要

sluicery は yt-dlp を用いた自己ホスト型のプレイリスト同期サーバー。
詳細は `docs/要件定義.md`。

## 現在の進捗

要件定義 §20 の実装順序に対する現在地。

- [x] 1. リポジトリ骨格、Docker 環境
- [x] 2. 設定読み込み、DB スキーマ
- [ ] 3. yt-dlp venv 管理と CLI ラッパ  ← 作業中
- [ ] 4. オプション合成モデル
- ...

## 直近の作業

- yt-dlp の venv 初期化処理を `src/sluicery/downloader/version.py` に実装
- `--progress-template` の JSON 出力パーサを作成。テストは3ケースのみ、異常系が未着手

## 次にやること

1. `progress.py` の異常系テストを追加（不完全な JSON 行、途中で切れた出力）
2. CLI ラッパのタイムアウト処理（要件 N-8）
3. §4 のオプション合成へ着手

## 未解決・保留

| # | 内容 | 状態 |
|---|---|---|
| 1 | rclone の SMB バックエンドで空き容量が取れるか未確認。取れない場合 `free_space()` は None を返す設計 | 要検証 |
| 2 | Windows 予約語のチェックを大文字小文字どちらで行うか未決 | 実装時に判断 |

## 重要な前提（忘れやすいもの）

- 配信元での削除は絶対にローカルファイルの削除に伝播させない
- `blocked` はリトライ回数を消費しない
- ファイル名の `[<source_id>]` は末尾（拡張子直前）に固定。relink がこれに依存している
- `SECRET_KEY` 未設定時は起動を拒否する
- push などのリモート git 操作は禁止

## 環境メモ

- 起動: `make up`
- テスト: `make test` または `pytest tests/`
- DB: `data/sluicery.db`（volume 内）
- マイグレーション: `alembic upgrade head`
- ログ: `data/logs/`

## 既知の落とし穴

- SQLite の WAL モードで、ワーカーとの同時書き込みが競合しやすい。書き込みトランザクションは短く保つ
- `docker compose down -v` は volume を消す。開発中は使わない
```

---

# 4. Git 運用

## 4.1 禁止事項

**以下のコマンドは実行しない。**

- `git push`（およびあらゆるリモートへの送信）
- `git remote add` / `git remote set-url`
- `git fetch` / `git pull` / `git clone`（外部リポジトリからの取得）
- `git push --force` を含む一切の force 操作
- `git reset --hard`（ユーザーの明示的な指示がある場合のみ可）
- `git clean -fdx`（同上）
- `git rebase` によるコミット済み履歴の書き換え（同上）

**ローカルで完結する操作は自由に使ってよい。** `add` / `commit` / `status` / `diff` / `log` / `branch` / `checkout` / `switch` / `stash` / `tag` / `restore` などは自由。

## 4.2 コミット方針

**こまめにコミットする。チェックポイントを多く残す。**

- 「動く状態」になったら、規模が小さくてもコミットする
- 大きな変更を1コミットにまとめない。1コミット＝1つの意味のある変更
- 実装とテストは同一コミットでよい
- ドキュメント更新は、対応する実装と同じコミットに含める
- リファクタリングと機能追加を同じコミットに混ぜない
- **壊れた状態でコミットしてよい。** ただしコミットメッセージに `wip:` を付け、何が壊れているか本文に書く。動く状態に戻ったら通常のコミットを積む

判断に迷ったら**コミット回数が多い方を選ぶ**。後からまとめるのは容易だが、分けるのは難しい。

## 4.3 コミットメッセージ

Conventional Commits 形式の prefix + 日本語の要約とする。

```
<type>: <日本語の要約（50文字程度）>

<本文。なぜその変更をしたか。何が未完了か>
```

`type` は以下を使う。

| type | 用途 |
|---|---|
| `feat` | 機能追加 |
| `fix` | バグ修正 |
| `refactor` | 振る舞いを変えない構造変更 |
| `test` | テストの追加・修正 |
| `docs` | ドキュメントのみの変更 |
| `chore` | ビルド、依存、設定 |
| `wip` | 未完成の途中経過 |

例：

```
feat: Storage の接続テストを4段階判定で実装

疎通・認証・一覧取得・書き込みを個別に判定し、どこで失敗したかを
UI に返せるようにした。rclone の終了コードだけでは判別できないため、
stderr のパターンマッチを併用している。

SMB 以外のバックエンドでのパターンは未検証。
```

## 4.4 ブランチとタグ

- 基本は `main` 上で作業してよい（単一開発者のため）
- 実験的な変更や、失敗した場合に丸ごと捨てたい変更は `feature/<名前>` を切る
- 要件定義 §20 の各段階を完了したら、**タグを打つ**：`checkpoint/step-03` のような形式
- タグはチェックポイントとしての意味を持つ。「ここまでは動いていた」を明示できる状態を維持する

## 4.5 コミット前の確認

コミット前に毎回以下を確認する。

1. `git status` で意図しないファイルが含まれていないか
2. `git diff --cached` で機密情報（パスワード、トークン、実際のホスト名、Cookie）が混入していないか
3. `.env` や `data/` 配下が含まれていないか

---

# 5. .gitignore

## 5.1 方針

**「除外し忘れ」より「含め忘れ」の方が安全**という前提で、広めに除外してから必要なものを明示的に許可する。

機密・状態・生成物は一切コミットしない。

## 5.2 初期内容

以下を `.gitignore` として作成する。プロジェクトの進行に応じて追加すること。

```gitignore
# ---- 機密 ----
.env
.env.*
!.env.example
*.key
*.pem
secrets/
rclone.conf
cookies.txt
*.cookies

# ---- 実行時データ ----
data/
staging/
media/
logs/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# ---- エディタ / OS ----
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# ---- その他 ----
*.log
tmp/
.tmp/
```

## 5.3 追加ルール

- **`.env.example` は必ずコミットする。** 全設定項目を既定値とコメント付きで記載し、実際の値は書かない
- `requirements.lock` はコミットする（再現性の要）
- `alembic/versions/` 配下のマイグレーションはコミットする
- 生成された rclone 設定ファイルは tmpfs 上に置き、リポジトリに現れない構造にする
- `docs/` 配下は全てコミットする
- 新しいディレクトリを作った際、そこに実行時データが入る可能性があれば**その場で `.gitignore` を更新する**

## 5.4 確認方法

除外が効いているかを確認する。

```bash
git check-ignore -v .env data/sluicery.db
git status --ignored          # 除外されているファイルの一覧
```

`.gitignore` を追加する前に既にコミットしてしまったファイルは、`git rm --cached <path>` でインデックスから外す。**その場合、履歴には残っている点をユーザーに報告すること**（機密が含まれていた場合は対処方針の判断を仰ぐ）。

---

# 6. コーディング規約

- フォーマッタ / リンタは `ruff`（format + lint）を使用する
- 型ヒントを付ける。`mypy` を通す（strict は必須としないが、新規コードは可能な限り厳しく）
- `subprocess` は `shell=True` を使わない。引数はリストで渡す（要件 N-10）
- 外部プロセスには必ずタイムアウトを設定する（要件 N-8）
- 例外を握り潰さない。ログに残すか、上位へ伝播させる
- ログにクレデンシャルを出さない。マスク処理は共通層に置き、各所での付け忘れが起きない構造にする
- コメントは日本語で書いてよい。「何を」ではなく「なぜ」を書く
- ユーザー向け文言（UI、エラーメッセージ）は日本語

---

# 7. テスト

- `pytest` を使用する
- 以下は必ずユニットテストを書く（要件定義 §20 で明示されているもの）
  - 状態遷移（`item.membership` / `target.status`）
  - オプション合成とガード（層の上書き順、予約引数の拒否、expert_mode）
  - ファイル名サニタイズ
  - relink ロジック
- 外部ネットワークに依存するテストは、既定でスキップされるマーカーを付ける（`@pytest.mark.network`）
- yt-dlp の実行を伴うテストはモックを基本とし、実行を伴うものは別マーカーに分離する
- テストが落ちている状態でタスクを完了扱いにしない。落ちたまま区切る場合は `wip:` コミットとし、AISTATE に明記する

---

# 8. 作業フロー

## 8.1 セッション開始時

1. `AISTATE.md` を読む
2. `git log --oneline -10` で直近の履歴を確認する
3. `git status` で作業ツリーの状態を確認する
4. 必要に応じて `docs/要件定義.md` の該当セクションを参照する
5. 作業内容をユーザーに宣言してから着手する

## 8.2 タスク実行中

1. 実装する
2. テストを書く / 動かす
3. `docs/基本設計.md` と `docs/変更履歴.md` を更新する
4. コミットする
5. 区切りが良ければ `AISTATE.md` を更新してコミットする

## 8.3 セッション終了時

以下を必ず実行する。

1. 作業ツリーをコミット可能な状態にする（未完了なら `wip:` でコミット）
2. **`AISTATE.md` を全文書き換える**
3. `docs/変更履歴.md` の未リリース欄を更新する
4. コミットする（`docs: AISTATE を更新` 等）
5. ユーザーに「次に何をすべきか」を1〜3行で伝える

## 8.4 判断に迷ったとき

- 要件定義 §1.4 の設計原則に照らす
- それでも決まらない場合、**ユーザーに確認する**。勝手に決めて進めない
- 確認を待つ間に進められる別作業があれば、そちらを進めてよい
- 確認の結果は `docs/基本設計.md` の「設計判断の記録」に残す

---

# 9. やってはいけないこと（再掲）

- リモートへの git 操作（push / fetch / pull / remote 設定）
- 要件定義に反する実装を、確認なしに進めること
- 機密情報のコミット
- `AISTATE.md` の更新を怠ったままセッションを終えること
- ドキュメント更新を「後でまとめて」に先送りすること
- 大きな変更を1コミットにまとめること
- 「良かれと思って」要件定義 §21 の非目標を実装すること
